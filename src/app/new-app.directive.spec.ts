import { NewAppDirective } from './new-app.directive';

describe('NewAppDirective', () => {
  it('should create an instance', () => {
    const directive = new NewAppDirective();
    expect(directive).toBeTruthy();
  });
});
